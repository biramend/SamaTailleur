{% extends 'base.html.twig' %}

{% block title %}Modele{% endblock %}

{% block body %}
    <h1>Modele</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ modele.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ modele.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ modele.description }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ modele.photo }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ modele.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('modele_index') }}">back to list</a>

    <a href="{{ path('modele_edit', {'id': modele.id}) }}">edit</a>

    {{ include('modele/_delete_form.html.twig') }}
{% endblock %}
